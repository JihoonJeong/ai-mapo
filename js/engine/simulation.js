/**
 * simulation.js — 시뮬레이션 엔진 (턴 틱 오케스트레이션)
 * Sprint 1: stub — Sprint 2에서 실제 수식 구현
 */

export function tick(gameState, playerActions) {
  // Sprint 2: 실제 시뮬레이션 구현 예정
  // 순서: 예산효과 → 정책효과 → 인구 → 경제 → 임대료 → 파급 → 재정 → 만족도
  console.log(`[Engine] Tick for turn ${gameState.meta.turn}`);
  return gameState;
}
